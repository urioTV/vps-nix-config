# Load the complete NixOS + Pulumi development environment.
use flake

# Alternative focused profiles:
# use flake .#nixos
# use flake .#pulumi
# use flake .#tools
# use flake .#ci

if [[ -f .envrc.local ]]; then
  source_env .envrc.local
fi
