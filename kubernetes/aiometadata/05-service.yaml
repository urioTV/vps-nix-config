apiVersion: v1
kind: Service
metadata:
  name: aiometadata
  namespace: aiometadata
spec:
  type: LoadBalancer
  loadBalancerIP: ${SERVICE_IP}
  ports:
    - port: 80
      targetPort: 3232
      name: http
  selector:
    app: aiometadata
